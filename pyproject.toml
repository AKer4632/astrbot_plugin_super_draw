[project]
name = "astrbot_plugin_super_draw"
version = "1.0.0"
description = "AstrBot 通用图像生成插件，支持多供应商、文生图、图生图、预设和 LLM 工具调用。"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.9.0",
    "openai>=2.37.0",
    "pillow>=10.0.0",
    "pydantic>=2.0.0",
]
